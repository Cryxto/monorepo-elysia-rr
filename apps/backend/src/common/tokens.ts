/**
 * Application-wide DI tokens
 *
 * These are string tokens used for dependency injection.
 * Separated from providers to avoid circular dependencies.
 */

export const API_SERVER = 'API_SERVER';
export const APP = 'APP';
